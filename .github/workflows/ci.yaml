name: CI - Train and Save the model (MLOPS POC1)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  matrix-pip:
    name: Train and Save the model
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: [3.11, 3.12. 3.13, 3.14]

    steps:
      - name: checkout
        uses: action/checkout@v6

      - name: setup python
        uses: actions/setup-python@v6
        with: 
          python-version: ${{matrix.python}}

      - name: Upgrade pip
        uses: python -m pip install --upgrade pip setuptools wheel

      - name: Install dependencies  
        uses: python -m pip install -r requirements.txt

      - name: Train the model
        uses: |
            python train.py
            ls -la artifacts

      - name: upload artifact
        uses: actions/upload-artifact@v7
        with:
           name: ml-artifact-${{matrix.python}}-${{github.run_id}}
           path: artifacts